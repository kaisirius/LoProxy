#include <state/ParseHeaderState.hpp>

ParseResult ParseHeaderState::handleHTTPparsing(const std::string& data, size_t bytes, int startIdx) {
    // TO-DO
    ParseResult res(Status(COMPLETE), 0);
    return res;
}