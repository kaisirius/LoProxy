#include <state/ParseReqLineState.hpp>

ParseResult ParseReqLineState::handleHTTPparsing(const std::string& data, size_t bytes, int startIdx) {
    // TO-DO
    ParseResult res(Status(COMPLETE), 0);
    return res;
}