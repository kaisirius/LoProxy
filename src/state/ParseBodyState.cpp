#include <state/ParseBodyState.hpp>

ParseResult ParseBodyState::handleHTTPparsing(const std::string& data, size_t bytes, int startIdx) {
    // TO-DO
    ParseResult res(Status(COMPLETE), 0);
    return res;
}